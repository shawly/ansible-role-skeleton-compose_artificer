---
# handlers file for {{ role_name }}

- name: Restart services
  become: true
  ansible.builtin.command:
    cmd: docker compose --progress=plain restart
    chdir: "{{ '{{ ' ~ role_name ~ '_install_dir.dir }}' }}"
  when: not _docker_compose.changed
